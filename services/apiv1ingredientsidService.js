export function updateIngredient(req, res) {
    res.send({
        message: 'This is the mockup controller for updateIngredient'
    });
}

export function deleteIngredient(req, res) {
    res.send({
        message: 'This is the mockup controller for deleteIngredient'
    });
}

